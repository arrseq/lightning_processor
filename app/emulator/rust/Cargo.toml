[package]
name = "atln-rust"
version = "0.1.0"
description = "Emulator bindings utility. Frame buffer tool, graphics, and IO pipeline system"
license = "MIT"
edition = "2018"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = "1"